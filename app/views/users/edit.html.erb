<%= form_with model: @user, local: true do |f| %>
  名前<%= f.text_field :name, autofocus: true, autocomplete: "name" %></br>
  メールアドレス<%= f.email_field :email, autocomplete: "email" %>
  <%= f.submit %>
<% end %>

<%= link_to "パスワードを編集", edit_user_registration_path %>